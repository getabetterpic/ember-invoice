<div class="row">
  <div class="col-md-3">
    <h1>Customers</h1>

    <ul>
     {{#each model}}
     <li>{{#link-to 'customer' this}}{{this.name}}{{/link-to}}</li>
      {{/each}}
    </ul>
  </div>
  <div class="col-md-9">
    {{outlet}}
  </div>
</div>